apiVersion: v1
kind: Service
metadata:
  name: serverless-dashboard-service
spec:
  selector:
    app: serverless-dashboard
  ports:
    - protocol: TCP
      port: 52001
      targetPort: 8050
  type: NodePort
