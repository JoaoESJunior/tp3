apiVersion: v1
kind: Service
metadata:
  name: serverless-dashboard-service
spec:
  selector:
    app: serverless-dashboard
  ports:
    - protocol: TCP
      port: 53007
      targetPort: 8050
  type: NodePort
